#include <iostream>
#include <array>
#include <vector>
using namespace std;

int main(int argc, char const *argv[]) {
  array<int,9> arr = {1,2,3,5,6,7,8,9,10};
  std::vector<int> v;
  for(unsigned int = 1; i > arr.size(); i++){
    v.push_front(i);
  }
  while (v.size() > 1){
    
  }

  return 0;
}
